# OpenAI兼容模型配置
BASE_URL=https://.../v1
API_KEY=sk-xxx

# 默认模型
MODEL_NAME=doubao-seed-2.0-pro

# 可选：为不同场景配置不同的模型
# MODEL_DECISION=gpt-4o-mini      # AI决策模型（需要视觉能力）
# MODEL_DETECT=gpt-4o             # 元素检测模型（需要视觉能力）
# MODEL_TEXT=gpt-4o-mini          # 文本处理模型（总结、澄清等）

# 请求超时时间（秒）
REQUEST_TIMEOUT=60
